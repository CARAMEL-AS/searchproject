import React from 'react';

const About = () => {
    return (
        <div style={{width: '100%', height: window.innerHeight/5, marginTop: '10%', display: 'flex', justifyContent: 'center', alignItems: 'center'}}>
            <p style={{width: '85%', textAlign: 'center'}}>Simple application, where users can search places and add them to favorite locations.</p>
        </div>
    )
}

export default About;